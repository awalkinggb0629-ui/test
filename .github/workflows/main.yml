name: Decompile AppleScript

on:
  workflow_dispatch:

jobs:
  decompile:
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Show file info
        run: |
          ls -l
          file 1.scpt

      - name: Run script
        run: |
          osascript 1.scpt test
          osascript 1.scpt flag
          osascript 1.scpt 1234
          strings -n 6 1.scpt
